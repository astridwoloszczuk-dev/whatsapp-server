{
  "name": "whatsapp-server",
  "version": "1.0.0",
  "description": "WhatsApp bridge for family automation — handles inbound messages (todos, calendar, birthday acks) and outbound message queue via Supabase",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.36.0",
    "@supabase/supabase-js": "^2.0.0",
    "dotenv": "^16.0.0",
    "googleapis": "^144.0.0",
    "qrcode-terminal": "^0.12.0",
    "whatsapp-web.js": "^1.23.0"
  }
}
